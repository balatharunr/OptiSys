LEGACY REFERENCE — primary data now stored in data/catalog/processes.catalog.json

✅ FULL EXPANDED — Safe to disable (home PCs, no feature use)

Grouped and simplified — copy-paste friendly.

A. Xbox / Gaming (safe if you do not use Xbox/GamePass/Game Bar)
GamingServices
GamingServicesNet
XboxGipSvc
XboxNetApiSvc
XblGameSave
GameInput
GameBarPresenceWriter
GameBar
XblAuthManager

B. Mixed Reality / VR (safe if no VR headset)
MixedRealityOpenXRSvc
HolographicShell
SpatialAudioService
MixedRealityRuntime
MixedRealityPortal

C. Printing / Fax (safe if no local printer / fax)
Spooler        # Print Spooler (disable if you truly have no printers)
Fax            # Fax service (rarely used)

D. Telemetry / Microsoft “UX” / Diagnostics (safe; you lose crash/telemetry)
DiagTrack             # Connected User Experiences & Telemetry
WerSvc / WerFault     # Windows Error Reporting service/process
WaaSMedicSvc?*        # Windows Update Medic — generally NOT recommended to disable (leave alone)


*Note: WaaSMedicSvc is not safe to permanently disable on many systems — skip disabling this one.

E. Phone Link / Device Sync / Push (safe if you don’t use Phone Link / notifications)
CDPSvc
CDPUserSvc_*         # CDP user instances
DeviceAssociationBroker
DevicePicker
WpnService
WpnUserService_*

F. Location / Maps (safe if you don’t use geolocation/Maps)
MapsBroker
lfsvc  # Geolocation Service

G. Ink / Touch / Tablet (safe if non-touch laptop)
TabletInputService    # TextInputHost, Touch keyboard
TouchKeyboardAndHandwritingPanelService

H. Developer / Telemetry / Helper services (usually safe)
RetailDemo
RemoteRegistry
Downloaded maps manager (MapsBroker handled above)

I. Performance helpers (optional; safe but may change perf behavior)
SysMain   # Superfetch (can be disabled on SSDs / problematic systems)

J. Misc optional items (safe in most home setups)
XblGameSave
GameInput
PhoneSvc (only if not using cellular features)

K. Doc Section A — Safe for everyone (2025 refresh)
dmwappushservice              # Windows Push Notifications bridge (Very Low)
Wercplsupport                 # Windows Error Reporting control panel (Very Low)
PcaSvc                        # Program Compatibility Assistant (Very Low)
WMPNetworkSvc                 # Windows Media Player network sharing (Very Low)
DiagnosticExecutionService    # Diagnostic execution helper (Very Low)
DiagnosticsHub.StandardCollector.Service    # Diagnostics Hub collector (Very Low)
MessagingService_*            # Messaging platform helper instances (Very Low)

L. Doc Section B — Safe if you don’t use the feature (2025 refresh)
RemoteAccess                  # Routing and Remote Access (remote support)
TermService                   # Remote Desktop Services core
UmRdpService                  # Remote Desktop user-mode port redirector
OneSyncSvc                    # Account/People sync host (mail/calendar)
\Microsoft\Office\OneDrive Standalone Update Task   # OneDrive scheduled updater task
wisvc                         # Windows Insider Service (disable if not enrolled)
bthserv / BTAGService / BluetoothUserService_*    # Bluetooth stack (disable only if you have zero BT devices)
icssvc / SharedAccess         # Mobile hotspot / Internet Connection Sharing
WSService / AppXSvc / StateRepository    # Microsoft Store / UWP platform infra
WSearch                       # Windows Search indexing (safe if you do not rely on indexing)

M. Doc Section D — Scheduled tasks (telemetry / maintenance)
\Microsoft\Windows\Application Experience\*                      # Telemetry and application tracking tasks (Very Low)
\Microsoft\Windows\Autochk\Proxy                                 # Disk check diagnostics trigger (Very Low)
\Microsoft\Windows\Customer Experience Improvement Program\*     # CEIP telemetry batch tasks (Very Low)
\Microsoft\Windows\Customer Experience Improvement Program\UsbCeip # Explicit USB CEIP task (Very Low)
\Microsoft\Windows\DiskDiagnostic\Microsoft-Windows-DiskDiagnosticDataCollector    # Disk diagnostic collector (Very Low)
\Microsoft\Windows\DiskFootprint\Diagnostics                     # Disk footprint analyzer (Very Low)
\Microsoft\Windows\Feedback\*                                    # Feedback hub telemetry (Very Low)
\Microsoft\Windows\Windows Error Reporting\*                     # Error reporting scheduled tasks (Very Low)
\Microsoft\Windows\Maintenance\WinSAT                           # System performance scoring (Very Low)
\Microsoft\Windows\Maps\*                                       # Maps and location scheduled jobs (Very Low)
\Microsoft\Windows\Mobile Broadband Accounts\*                  # Mobile broadband account maintenance (Very Low)

⚠️ Items to treat with CAUTION (don’t disable unless you know you don’t need them)

These are sometimes safe but can break networking/updates/Windows features:

iphlpsvc (IP Helper) — safe if you don’t use IPv6 / Teredo / portproxy features; otherwise keep.

BITS (Background Intelligent Transfer Service) — used by some installers/Windows Update/certain apps; prefer Manual if testing.

InstallService — Media/streaming app install helper; disable only if you never use groove/movies & tv.

SensorDataService / SensorService / SensrSvc — Sensor platform stack (accelerometer/rotation features) for tablets.

NcbService / NfcService — Near-field communication helpers; disable only if the PC has zero NFC/mobile features.

AppVClient / AADBrokerPlugin / EnterpriseAppManagementService / ConsentUxUserSvc_* — Enterprise/Workplace bridge services (may break workplace join or virtualized apps).

CloudFilesService / SyncHost / ClipSVC — Cloud clipboard, OneDrive placeholders, and Store licensing (expect clipboard/store regressions).

wuauserv (Windows Update) — DO NOT disable.

winmgmt (WMI) — required for many tools; do not disable.

🔁 Quick safe workflow (PowerShell): backup, disable, revert easily

Run as admin.

1) Export current service config (backup):

# backup current start types
Get-Service | Select-Object Name, Status, @{n='StartType';e={(Get-CimInstance Win32_Service -Filter "Name='$_'").StartMode}} |
  Export-Csv C:\service-backup.csv -NoTypeInformation


2) Disable a service safely (example SysMain):

# set to Manual first for testing
Set-Service -Name SysMain -StartupType Manual
# if all good after reboot, set Disabled:
Set-Service -Name SysMain -StartupType Disabled


3) Disable multiple safe services (example list):

$safe = @('GamingServices','GamingServicesNet','XboxGipSvc','XboxNetApiSvc','XblGameSave','GameBar','WerSvc','DiagTrack','RemoteRegistry','SysMain')
foreach($s in $safe){
    try{
        Set-Service -Name $s -StartupType Disabled -ErrorAction Stop
    } catch {
        Write-Warning "Could not change $s: $_"
    }
}


4) Revert from backup:

Import-Csv C:\service-backup.csv | ForEach-Object {
  $name = $_.Name
  $mode = $_.StartType
  if ($mode -in @('Auto','Automatic')) { Set-Service -Name $name -StartupType Automatic }
  elseif ($mode -in @('Manual')) { Set-Service -Name $name -StartupType Manual }
  elseif ($mode -in @('Disabled')) { Set-Service -Name $name -StartupType Disabled }
}

🧾 Extra checks before toggling a service

See dependent services:

Get-Service -Name <ServiceName> | Select-Object -ExpandProperty DependentServices


Check executable path (confirm it’s genuine):

Get-CimInstance Win32_Service -Filter "Name='SysMain'" | Select-Object Name, PathName