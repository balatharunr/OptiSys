packages:
    - id: docker-desktop
      name: Docker Desktop
      manager: winget
      command: "winget install --id Docker.DockerDesktop -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: true
      summary: Container management for Windows.
      homepage: "https://www.docker.com/products/docker-desktop"
      tags: [containers]
    - id: azure-cli
      name: Azure CLI
      manager: winget
      command: "winget install --id Microsoft.AzureCLI -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Manage Azure resources from the command line.
      homepage: "https://learn.microsoft.com/cli/azure"
      tags: [cloud]
    - id: aws-cli
      name: AWS CLI
      manager: winget
      command: "winget install --id Amazon.AWSCLI -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Manage AWS resources via CLI.
      homepage: "https://aws.amazon.com/cli/"
      tags: [cloud]
    - id: terraform
      name: Terraform
      manager: winget
      command: "winget install --id Hashicorp.Terraform -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Infrastructure as code CLI.
      homepage: "https://www.terraform.io"
      tags: [iac]
    - id: kubectl
      name: kubectl
      manager: winget
      command: "winget install --id Kubernetes.kubectl -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Kubernetes cluster management CLI.
      homepage: "https://kubernetes.io/docs/reference/kubectl/"
      tags: [kubernetes]
    - id: helm
      name: Helm
      manager: winget
      command: "winget install --id Helm.Helm -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Package manager for Kubernetes charts.
      homepage: "https://helm.sh"
      tags: [kubernetes]
    - id: azure-storage-explorer
      name: Azure Storage Explorer
      manager: choco
      command: "choco install microsoftazurestorageexplorer -y --no-progress"
      requiresAdmin: true
      summary: Manage Azure storage accounts visually.
      homepage: "https://learn.microsoft.com/azure/storage/common/storage-explorer"
      tags: [cloud]
    - id: podman
      name: Podman Desktop
      manager: winget
      command: "winget install --id RedHat.Podman-Desktop -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: true
      summary: Rootless container workflows.
      homepage: "https://podman.io"
      tags: [containers]
    - id: k3d
      name: k3d
      manager: winget
      command: "winget install --id k3d.k3d -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Lightweight wrapper to run K3s Kubernetes clusters in Docker.
      homepage: "https://k3d.io"
      tags: [kubernetes]
    - id: kind
      name: kind
      manager: choco
      command: "choco install kind -y --no-progress"
      requiresAdmin: true
      summary: Kubernetes-in-Docker tool for local cluster testing.
      homepage: "https://kind.sigs.k8s.io"
      tags: [kubernetes]
    - id: trivy
      name: Trivy
      manager: winget
      command: "winget install --id AquaSecurity.Trivy -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Vulnerability scanner for containers, IaC, and cloud artifacts.
      homepage: "https://aquasecurity.github.io/trivy"
      tags: [security]
    - id: dive
      name: Dive
      manager: winget
      command: "winget install --id wagoodman.dive -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Inspect container image layers to analyze contents and size.
      homepage: "https://github.com/wagoodman/dive"
      tags: [containers]
    - id: istioctl
      name: istioctl
      manager: choco
      command: "choco install istioctl -y --no-progress"
      requiresAdmin: true
      summary: Command-line utility for managing Istio service meshes.
      homepage: "https://istio.io/latest/docs/reference/commands/istioctl/"
      tags: [service-mesh]
    - id: skaffold
      name: Skaffold
      manager: choco
      command: "choco install skaffold -y --no-progress"
      requiresAdmin: true
      summary: Automates local Kubernetes development workflows.
      homepage: "https://skaffold.dev"
      tags: [kubernetes, devops]
    - id: pulumi
      name: Pulumi
      manager: choco
      command: "choco install pulumi -y --no-progress"
      requiresAdmin: true
      summary: Infrastructure-as-code platform using familiar programming languages.
      homepage: "https://www.pulumi.com"
      tags: [iac]
    - id: k9s
      name: k9s
      manager: choco
      command: "choco install k9s -y --no-progress"
      requiresAdmin: true
      summary: Terminal UI to interact with Kubernetes clusters efficiently.
      homepage: "https://k9scli.io"
      tags: [kubernetes]
    - id: kubectx
      name: kubectx
      manager: choco
      command: "choco install kubectx -y --no-progress"
      requiresAdmin: true
      summary: Fast kubeconfig context switching helper for kubectl.
      homepage: "https://github.com/ahmetb/kubectx"
      tags: [kubernetes]
    - id: kubens
      name: kubens
      manager: choco
      command: "choco install kubens -y --no-progress"
      requiresAdmin: true
      summary: Quick namespace switcher companion to kubectx.
      homepage: "https://github.com/ahmetb/kubectx"
      tags: [kubernetes]
    - id: openshift-cli
      name: OpenShift CLI (oc)
      manager: choco
      command: "choco install openshift-cli -y --no-progress"
      requiresAdmin: true
      summary: Command-line tooling for Red Hat OpenShift clusters.
      homepage: "https://docs.openshift.com/container-platform/latest/cli_reference/openshift_cli/getting-started-cli.html"
      tags: [kubernetes]
    - id: jenkins
      name: Jenkins
      manager: choco
      command: "choco install jenkins -y --no-progress"
      requiresAdmin: true
      summary: Automation server for building, deploying, and automating projects.
      homepage: "https://www.jenkins.io"
      tags: [devops]
    - id: jq
      name: jq
      manager: winget
      command: "winget install --id jqlang.jq -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Lightweight JSON processor.
      homepage: "https://stedolan.github.io/jq"
      tags: [cli]
    - id: minikube
      name: minikube
      manager: winget
      command: "winget install --id Kubernetes.minikube -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: true
      summary: Run a local Kubernetes cluster.
      homepage: "https://minikube.sigs.k8s.io"
      tags: [kubernetes]
    - id: virtualbox
      name: Oracle VirtualBox
      manager: winget
      command: "winget install --id Oracle.VirtualBox -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: true
      summary: Desktop virtualization platform.
      homepage: "https://www.virtualbox.org"
      tags: [virtualization]
    - id: vmware-player
      name: VMware Workstation Player
      manager: choco
      command: "choco install vmware-workstation-player -y --no-progress"
      requiresAdmin: true
      summary: VMware desktop hypervisor for labs.
      homepage: "https://www.vmware.com/products/workstation-player.html"
      tags: [virtualization]
    - id: aws-sam-cli
      name: AWS SAM CLI
      manager: choco
      command: "choco install aws-sam-cli -y --no-progress"
      requiresAdmin: true
      summary: Build and deploy serverless applications on AWS.
      homepage: "https://aws.amazon.com/serverless/sam/"
      tags: [aws, serverless]
    - id: azure-functions-core-tools
      name: Azure Functions Core Tools 4
      manager: choco
      command: "choco install azure-functions-core-tools-4 -y --no-progress"
      requiresAdmin: true
      summary: Local development tools for Azure Functions.
      homepage: "https://learn.microsoft.com/azure/azure-functions/functions-run-local"
      tags: [azure, serverless]
    - id: consul
      name: HashiCorp Consul
      manager: winget
      command: "winget install --id Hashicorp.Consul -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: true
      summary: Service discovery and service mesh runtime.
      homepage: "https://www.consul.io"
      tags: [service-mesh]
    - id: nomad
      name: HashiCorp Nomad
      manager: winget
      command: "winget install --id Hashicorp.Nomad -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: true
      summary: Flexible orchestrator for batch and service workloads.
      homepage: "https://www.nomadproject.io"
      tags: [orchestration]
    - id: dapr-cli
      name: Dapr CLI
      manager: winget
      command: "winget install --id Dapr.CLI -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Local tooling for the Distributed Application Runtime.
      homepage: "https://dapr.io"
      tags: [microservices]
    - id: talosctl
      name: talosctl
      manager: choco
      command: "choco install talosctl -y --no-progress"
      requiresAdmin: true
      summary: Management CLI for Talos Linux Kubernetes operating system.
      homepage: "https://www.talos.dev"
      tags: [kubernetes]
    - id: google-cloud-sdk
      name: Google Cloud SDK
      manager: winget
      command: "winget install --id Google.CloudSDK -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: true
      summary: CLI tooling for managing Google Cloud resources.
      homepage: "https://cloud.google.com/sdk"
      tags: [cloud]
    - id: rancher-desktop
      name: Rancher Desktop
      manager: winget
      command: "winget install --id SUSE.RancherDesktop -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: true
      summary: Kubernetes and container management alternative to Docker Desktop.
      homepage: "https://rancherdesktop.io"
      tags: [kubernetes]
    - id: podman-desktop
      name: Podman Desktop
      manager: winget
      command: "winget install --id RedHat.Podman-Desktop -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: true
      summary: Desktop application for managing Podman containers.
      homepage: "https://podman-desktop.io"
      tags: [containers, desktop]
    - id: rclone
      name: rclone
      manager: winget
      command: "winget install --id Rclone.Rclone -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Command-line tool to sync files across local and cloud storage providers.
      homepage: "https://rclone.org"
      tags: [cloud]
    - id: portainer
      name: Portainer
      manager: scoop
      command: "scoop install portainer"
      requiresAdmin: false
      buckets:
          - main
      summary: Container management UI for Docker and Kubernetes.
      homepage: "https://www.portainer.io"
      tags: [containers, ui]
    - id: kustomize
      name: Kustomize
      manager: choco
      command: "choco install kustomize -y --no-progress"
      requiresAdmin: true
      summary: Kubernetes native configuration management.
      homepage: "https://kustomize.io"
      tags: [kubernetes, configuration]
    - id: lens
      name: Lens
      manager: winget
      command: "winget install --id Mirantis.Lens -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Kubernetes IDE for managing clusters.
      homepage: "https://k8slens.dev"
      tags: [kubernetes, ide]
    - id: hadolint
      name: Hadolint
      manager: choco
      command: "choco install hadolint -y --no-progress"
      requiresAdmin: true
      summary: Dockerfile linter for best practices.
      homepage: "https://hadolint.github.io/hadolint"
      tags: [containers, linting]
    - id: buildx
      name: Docker Buildx
      manager: winget
      command: "winget install --id Docker.DockerCLI -e --accept-package-agreements --accept-source-agreements"
      requiresAdmin: false
      summary: Extended build capabilities for Docker.
      homepage: "https://docs.docker.com/buildx"
      tags: [containers, build]
    - id: kompose
      name: Kompose
      manager: choco
      command: "choco install kubernetes-kompose -y --no-progress"
      requiresAdmin: true
      summary: Tool to convert Docker Compose to Kubernetes.
      homepage: "https://kompose.io"
      tags: [kubernetes, migration]
    - id: tilt
      name: Tilt
      manager: choco
      command: "choco install tilt -y --no-progress"
      requiresAdmin: true
      summary: Development environment for Kubernetes applications.
      homepage: "https://tilt.dev"
      tags: [kubernetes, development]
    - id: istio
      name: Istio
      manager: choco
      command: "choco install istio -y --no-progress"
      requiresAdmin: true
      summary: Service mesh for microservices.
      homepage: "https://istio.io"
      tags: [service-mesh, microservices]
    - id: linkerd
      name: Linkerd
      manager: choco
      command: "choco install linkerd -y --no-progress"
      requiresAdmin: true
      summary: Lightweight service mesh for Kubernetes.
      homepage: "https://linkerd.io"
      tags: [service-mesh, kubernetes]
    - id: envoy
      name: Envoy Proxy
      manager: choco
      command: "choco install envoy -y --no-progress"
      requiresAdmin: true
      summary: High-performance edge and service proxy.
      homepage: "https://envoyproxy.io"
      tags: [proxy, microservices]
    - id: consul-template
      name: Consul Template
      manager: choco
      command: "choco install consul-template -y --no-progress"
      requiresAdmin: true
      summary: Template rendering with Consul data.
      homepage: "https://github.com/hashicorp/consul-template"
      tags: [consul, templating]
    - id: consul-connect
      name: Consul Connect
      manager: choco
      command: "choco install consul-connect -y --no-progress"
      requiresAdmin: true
      summary: Service mesh solution from HashiCorp.
      homepage: "https://www.consul.io/docs/connect"
      tags: [service-mesh, consul]
    - id: consul-k8s
      name: Consul on Kubernetes
      manager: choco
      command: "choco install consul-k8s -y --no-progress"
      requiresAdmin: true
      summary: Consul service mesh for Kubernetes.
      homepage: "https://www.consul.io/docs/k8s"
      tags: [consul, kubernetes]
    - id: vault-k8s
      name: Vault on Kubernetes
      manager: choco
      command: "choco install vault-k8s -y --no-progress"
      requiresAdmin: true
      summary: Vault secrets management for Kubernetes.
      homepage: "https://www.vaultproject.io/docs/platform/k8s"
      tags: [vault, kubernetes]
    - id: nomad-k8s
      name: Nomad on Kubernetes
      manager: choco
      command: "choco install nomad-k8s -y --no-progress"
      requiresAdmin: true
      summary: Nomad workload orchestrator for Kubernetes.
      homepage: "https://www.nomadproject.io/docs/kubernetes"
      tags: [nomad, kubernetes]
    - id: waypoint-k8s
      name: Waypoint on Kubernetes
      manager: choco
      command: "choco install waypoint-k8s -y --no-progress"
      requiresAdmin: true
      summary: Waypoint application delivery for Kubernetes.
      homepage: "https://www.waypointproject.io/docs/kubernetes"
      tags: [waypoint, kubernetes]
    - id: boundary-k8s
      name: Boundary on Kubernetes
      manager: choco
      command: "choco install boundary-k8s -y --no-progress"
      requiresAdmin: true
      summary: Boundary secure access for Kubernetes.
      homepage: "https://www.boundaryproject.io/docs/kubernetes"
      tags: [boundary, kubernetes]
    - id: terraform-k8s
      name: Terraform Kubernetes Provider
      manager: choco
      command: "choco install terraform-k8s -y --no-progress"
      requiresAdmin: true
      summary: Terraform provider for Kubernetes.
      homepage: "https://registry.terraform.io/providers/hashicorp/kubernetes"
      tags: [terraform, kubernetes]
    - id: pulumi-k8s
      name: Pulumi Kubernetes
      manager: choco
      command: "choco install pulumi-k8s -y --no-progress"
      requiresAdmin: true
      summary: Pulumi provider for Kubernetes.
      homepage: "https://www.pulumi.com/registry/packages/kubernetes"
      tags: [pulumi, kubernetes]
    - id: ansible-k8s
      name: Ansible Kubernetes
      manager: choco
      command: "choco install ansible-k8s -y --no-progress"
      requiresAdmin: true
      summary: Ansible modules for Kubernetes.
      homepage: "https://docs.ansible.com/ansible/latest/collections/kubernetes/core"
      tags: [ansible, kubernetes]
    - id: chef-k8s
      name: Chef Kubernetes
      manager: choco
      command: "choco install chef-k8s -y --no-progress"
      requiresAdmin: true
      summary: Chef cookbooks for Kubernetes.
      homepage: "https://supermarket.chef.io/cookbooks/kubernetes"
      tags: [chef, kubernetes]
    - id: puppet-k8s
      name: Puppet Kubernetes
      manager: choco
      command: "choco install puppet-k8s -y --no-progress"
      requiresAdmin: true
      summary: Puppet modules for Kubernetes.
      homepage: "https://forge.puppet.com/modules/puppetlabs/kubernetes"
      tags: [puppet, kubernetes]
    - id: salt-k8s
      name: Salt Kubernetes
      manager: choco
      command: "choco install salt-k8s -y --no-progress"
      requiresAdmin: true
      summary: Salt states for Kubernetes.
      homepage: "https://docs.saltproject.io/en/latest/ref/states/all/salt.states.kubernetes"
      tags: [salt, kubernetes]
    - id: gitlab-k8s
      name: GitLab Kubernetes
      manager: choco
      command: "choco install gitlab-k8s -y --no-progress"
      requiresAdmin: true
      summary: GitLab integration with Kubernetes.
      homepage: "https://docs.gitlab.com/ee/user/clusters/kubernetes"
      tags: [gitlab, kubernetes]
    - id: github-k8s
      name: GitHub Kubernetes
      manager: choco
      command: "choco install github-k8s -y --no-progress"
      requiresAdmin: true
      summary: GitHub Actions for Kubernetes.
      homepage: "https://github.com/actions/kubernetes"
      tags: [github, kubernetes]
