<UserControl x:Class="OptiSys.App.Controls.KnownProcessesPivotTitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:OptiSys.App.ViewModels"
             xmlns:converters="clr-namespace:OptiSys.App.Converters"
             Height="44"
             MinWidth="420"
             VerticalAlignment="Center">
    <UserControl.Resources>
        <converters:EnumEqualsConverter x:Key="EnumEqualsConverter"/>

        <Style x:Key="PivotButtonStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="18,7"/>
            <Setter Property="Margin" Value="6,0"/>
            <Setter Property="MinWidth" Value="138"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="Background" Value="#0F1C30"/>
            <Setter Property="BorderBrush" Value="#1F2E46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#2563EB" Offset="0"/>
                                            <GradientStop Color="#1D4ED8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#60A5FA"/>
                                <Setter Property="Foreground" Value="#F8FAFC"/>
                                <Setter TargetName="Root" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="14" ShadowDepth="0" Opacity="0.25" Color="#60A5FA"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#38BDF8"/>
                                <Setter TargetName="Root" Property="Background" Value="#13294A"/>
                                <Setter Property="Foreground" Value="#F8FAFC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Rectangle x:Name="SweepRect"
                   Height="3"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Stretch"
                   Margin="0,0,0,2"
                   Panel.ZIndex="0"
                   IsHitTestVisible="False"/>

        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Margin="6,0,6,0"
                    Panel.ZIndex="1">
            <TextBlock Text="View:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"
                       Foreground="#CBD5F5"
                       FontWeight="SemiBold"/>
            <ToggleButton Content="Known processes"
                          Style="{StaticResource PivotButtonStyle}"
                          Command="{Binding SwitchSectionCommand}"
                          CommandParameter="{x:Static viewModels:KnownProcessViewSection.Catalog}"
                          IsChecked="{Binding ActiveSection, Converter={StaticResource EnumEqualsConverter}, ConverterParameter={x:Static viewModels:KnownProcessViewSection.Catalog}}"/>
            <ToggleButton Content="Process controls"
                          Style="{StaticResource PivotButtonStyle}"
                          Command="{Binding SwitchSectionCommand}"
                          CommandParameter="{x:Static viewModels:KnownProcessViewSection.Settings}"
                          IsChecked="{Binding ActiveSection, Converter={StaticResource EnumEqualsConverter}, ConverterParameter={x:Static viewModels:KnownProcessViewSection.Settings}}"/>
            <ToggleButton Content="Threat Watch"
                          Style="{StaticResource PivotButtonStyle}"
                          Command="{Binding SwitchSectionCommand}"
                          CommandParameter="{x:Static viewModels:KnownProcessViewSection.ThreatWatch}"
                          IsChecked="{Binding ActiveSection, Converter={StaticResource EnumEqualsConverter}, ConverterParameter={x:Static viewModels:KnownProcessViewSection.ThreatWatch}}"/>
        </StackPanel>
    </Grid>
</UserControl>
