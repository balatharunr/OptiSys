<UserControl x:Class="OptiSys.App.Views.InstallHubBundlesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:OptiSys.App.ViewModels"
             xmlns:ux="clr-namespace:OptiSys.App.Controls"
             xmlns:infra="clr-namespace:OptiSys.App.Infrastructure"
             mc:Ignorable="d"
             Background="Transparent"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="Auto"
             TextOptions.TextHintingMode="Fixed">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="InstallHubResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid x:Name="BundlesLayoutGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="BundlesPrimaryColumnHost"
                    Grid.Row="0"
                    Margin="0">
            <Border Style="{StaticResource InstallCardBorderStyle}"
                    Padding="12,16,12,18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0"
                          Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Bundle library"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource InstallTextPrimaryBrush}"/>
                            <TextBlock Text="Select a bundle to inspect or queue the whole stack"
                                       Foreground="{StaticResource InstallTextMutedBrush}"
                                       FontSize="12"/>
                        </StackPanel>
                        <Button Grid.Column="1"
                                Content="Browse catalog"
                                Command="{Binding NavigatePivotCommand}"
                                CommandParameter="{x:Static viewModels:CurrentInstallHubPivot.Catalog}"
                                Style="{StaticResource InstallSecondaryButtonStyle}"/>
                    </Grid>

                    <Grid Grid.Row="1">
                        <ItemsControl ItemsSource="{StaticResource InstallHubBundleSkeletonItems}"
                                      ItemTemplate="{StaticResource InstallHubBundleSkeletonTemplate}">
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>

                        <ListBox x:Name="BundlesList"
                                 ItemsSource="{Binding Bundles}"
                                 SelectedItem="{Binding SelectedBundle, Mode=TwoWay}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="0"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.CanContentScroll="True"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling"
                                 VirtualizingPanel.CacheLength="2"
                                 infra:ScrollHost.BubbleParentScroll="True">
                            <ListBox.Style>
                                <Style TargetType="ListBox">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.Style>
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ux:AdaptiveTilePanel MinColumnWidth="340" MaxColumnWidth="520" ColumnSpacing="16" RowSpacing="16" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <StaticResource ResourceKey="InstallBundleCardItemStyle"/>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                        <Border Style="{StaticResource InstallInteractiveCardBorderStyle}"
                                            Padding="20">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <StackPanel Grid.Row="0">
                                                <StackPanel Orientation="Horizontal"
                                                            VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Name}"
                                                               FontSize="16"
                                                               FontWeight="SemiBold"
                                                               Foreground="{StaticResource InstallTextPrimaryBrush}"
                                                               TextWrapping="Wrap"/>
                                                    <Border Margin="10,0,0,0"
                                                            Visibility="{Binding IsSyntheticAll, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                            Background="{StaticResource InstallBadgeAltBackgroundBrush}"
                                                            CornerRadius="8"
                                                            Padding="8,3">
                                                        <TextBlock Text="All packages"
                                                                   Foreground="{StaticResource InstallTextAccentBrush}"
                                                                   FontSize="11"
                                                                   FontWeight="SemiBold"/>
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock Text="{Binding Description}"
                                                           Foreground="{StaticResource InstallTextMutedBrush}"
                                                           FontSize="12"
                                                           Margin="0,6,0,0"
                                                           TextWrapping="Wrap"/>
                                            </StackPanel>

                                            <WrapPanel Grid.Row="1"
                                                       Margin="0,12,0,0">
                                                <Border>
                                                    <TextBlock Text="{Binding PackageCountDisplay}"
                                                                   Foreground="{StaticResource InstallTextSecondaryBrush}"
                                                               FontSize="11"
                                                               FontWeight="SemiBold"/>
                                                    <Border.Style>
                                                        <Style TargetType="Border"
                                                               BasedOn="{StaticResource InstallBadgeStyle}">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSyntheticAll}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                </Border>
                                                <Border Style="{StaticResource InstallBadgeStyle}"
                                                        Background="{StaticResource InstallBadgeAltBackgroundBrush}"
                                                        Visibility="{Binding IsSyntheticAll, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <TextBlock Text="{Binding BundlePackageCount, StringFormat={}{0} packages}"
                                                                   Foreground="{StaticResource InstallTextSecondaryBrush}"
                                                               FontSize="11"
                                                               FontWeight="SemiBold"/>
                                                </Border>
                                            </WrapPanel>

                                            <StackPanel Grid.Row="2"
                                                        Orientation="Horizontal"
                                                        Margin="0,16,0,0">
                                                <Button Content="Queue bundle"
                                                        Command="{Binding DataContext.QueueBundleCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                        CommandParameter="{Binding}"
                                                        MinWidth="96">
                                                    <Button.Style>
                                                        <Style TargetType="Button"
                                                               BasedOn="{StaticResource InstallAccentButtonStyle}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSyntheticAll}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                                <Button Content="Browse catalog"
                                                        Margin="8,0,0,0"
                                                        Command="{Binding DataContext.ViewBundleDetailsCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource InstallSecondaryButtonStyle}"
                                                        MinWidth="110"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <TextBlock Text="No bundles available yet. Refresh the catalog to add definitions."
                                   Foreground="{StaticResource InstallTextCaptionBrush}"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   Margin="0,20,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Bundles.Count}" Value="0"/>
                                                <Condition Binding="{Binding IsLoading}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
