<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:TidyWindow.App.Converters"
                    xmlns:viewModels="clr-namespace:TidyWindow.App.ViewModels"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    <converters:EnumEqualsConverter x:Key="EnumEqualsConverter"/>
    <SolidColorBrush x:Key="InstallCardBackgroundBrush" Color="#101A2B"/>
    <SolidColorBrush x:Key="InstallAccentButtonBrush" Color="#43B9F5"/>
    <SolidColorBrush x:Key="InstallAccentButtonHoverBrush" Color="#58C9FF"/>
    <SolidColorBrush x:Key="InstallAccentButtonPressedBrush" Color="#2F96CC"/>
    <SolidColorBrush x:Key="InstallSecondaryButtonBrush" Color="#172238"/>
    <SolidColorBrush x:Key="InstallSecondaryButtonHoverBrush" Color="#1E2D46"/>
    <SolidColorBrush x:Key="InstallSecondaryButtonPressedBrush" Color="#10182A"/>
    <SolidColorBrush x:Key="InstallRetryButtonBrush" Color="#FF6B87"/>
    <SolidColorBrush x:Key="InstallRetryButtonHoverBrush" Color="#FF7A97"/>
    <SolidColorBrush x:Key="InstallRetryButtonPressedBrush" Color="#E25471"/>
    <SolidColorBrush x:Key="InstallClearButtonBrush" Color="#14B8A6"/>
    <SolidColorBrush x:Key="InstallClearButtonHoverBrush" Color="#1FD1BB"/>
    <SolidColorBrush x:Key="InstallClearButtonPressedBrush" Color="#0F9A87"/>
    <SolidColorBrush x:Key="InstallTextPrimaryBrush" Color="#F8FAFC"/>
    <SolidColorBrush x:Key="InstallTextInverseBrush" Color="#E2E8F0"/>
    <SolidColorBrush x:Key="InstallTextSecondaryBrush" Color="#CBD5F5"/>
    <SolidColorBrush x:Key="InstallTextMutedBrush" Color="#8FB0D9"/>
    <SolidColorBrush x:Key="InstallTextCaptionBrush" Color="#64748B"/>
    <SolidColorBrush x:Key="InstallTextAccentBrush" Color="#38BDF8"/>
    <SolidColorBrush x:Key="InstallTextSoftAccentBrush" Color="#93C5FD"/>
    <SolidColorBrush x:Key="InstallTextInfoBrush" Color="#7FB7D6"/>
    <SolidColorBrush x:Key="InstallTextTimestampBrush" Color="#475569"/>
    <SolidColorBrush x:Key="InstallTextSuccessBrush" Color="#22C55E"/>
    <SolidColorBrush x:Key="InstallTextWarningBrush" Color="#FBBF24"/>
    <SolidColorBrush x:Key="InstallTextWarningStrongBrush" Color="#F97316"/>
    <SolidColorBrush x:Key="InstallTextQueuedBrush" Color="#FDE68A"/>
    <SolidColorBrush x:Key="InstallTextDangerBrush" Color="#F87171"/>
    <SolidColorBrush x:Key="InstallTextDangerSoftBrush" Color="#FCA5A5"/>
    <SolidColorBrush x:Key="InstallTextDangerAccentBrush" Color="#FF6B87"/>
    <SolidColorBrush x:Key="InstallBadgeBackgroundBrush" Color="#1A2D45"/>
    <SolidColorBrush x:Key="InstallBadgeAltBackgroundBrush" Color="#1B2A44"/>
    <SolidColorBrush x:Key="InstallBadgeDarkBackgroundBrush" Color="#1F2937"/>
    <SolidColorBrush x:Key="InstallBadgeQueueBackgroundBrush" Color="#12263F"/>
    <SolidColorBrush x:Key="InstallBadgeNeutralBackgroundBrush" Color="#1D2337"/>
    <SolidColorBrush x:Key="InstallBadgeErrorBackgroundBrush" Color="#331A1F"/>
    <SolidColorBrush x:Key="InstallCardHoverBackgroundBrush" Color="#192C48"/>
    <SolidColorBrush x:Key="InstallCardHoverBorderBrush" Color="#3F5F97"/>
    <SineEase x:Key="InstallCardHoverEase" EasingMode="EaseOut"/>

    <Style x:Key="InstallAccentButtonStyle"
           TargetType="Button">
        <Setter Property="Background" Value="{StaticResource InstallAccentButtonBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource InstallTextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,7"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect Color="#4E7BFF"
                                              BlurRadius="18"
                                              ShadowDepth="0"
                                              Opacity="0.35"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallAccentButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallAccentButtonPressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background" Value="#2E4F66"/>
                            <Setter Property="Foreground" Value="#9FB9CC"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallRetryButtonStyle"
           TargetType="Button">
        <Setter Property="Background" Value="{StaticResource InstallRetryButtonBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource InstallTextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,7"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect Color="#FF97AF"
                                              BlurRadius="18"
                                              ShadowDepth="0"
                                              Opacity="0.35"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallRetryButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallRetryButtonPressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background" Value="#8E4A5C"/>
                            <Setter Property="Foreground" Value="#FBD3DC"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallClearButtonStyle"
           TargetType="Button">
        <Setter Property="Background" Value="{StaticResource InstallClearButtonBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource InstallTextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,7"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect Color="#2BD7BB"
                                              BlurRadius="18"
                                              ShadowDepth="0"
                                              Opacity="0.35"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallClearButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallClearButtonPressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background" Value="#0B5649"/>
                            <Setter Property="Foreground" Value="#B2EFE3"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallSecondaryButtonStyle"
           TargetType="Button">
        <Setter Property="Background" Value="{StaticResource InstallSecondaryButtonBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource InstallTextInverseBrush}"/>
        <Setter Property="BorderBrush" Value="#2A3C5A"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallSecondaryButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallSecondaryButtonPressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background" Value="#0F1626"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="#1B283E"/>
                            <Setter Property="Foreground" Value="{StaticResource InstallTextTimestampBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallSecondaryToggleButtonStyle"
           TargetType="ToggleButton">
        <Setter Property="Background" Value="{StaticResource InstallSecondaryButtonBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource InstallTextInverseBrush}"/>
        <Setter Property="BorderBrush" Value="#2A3C5A"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallSecondaryButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallSecondaryButtonPressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="#38BDF8"/>
                            <Setter TargetName="Root" Property="Background" Value="#172337"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background" Value="#0F1626"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="#1B283E"/>
                            <Setter Property="Foreground" Value="{StaticResource InstallTextTimestampBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallBadgeStyle"
           TargetType="Border">
        <Setter Property="Background" Value="{StaticResource InstallBadgeBackgroundBrush}"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Margin" Value="0,0,8,8"/>
    </Style>

    <Style x:Key="InstallInfoChipStyle"
           TargetType="Border"
           BasedOn="{StaticResource InstallBadgeStyle}">
        <Setter Property="Background" Value="#10263A"/>
        <Setter Property="BorderBrush" Value="#21405B"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="Margin" Value="0,0,10,10"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

        <Style x:Key="InstallCardBorderStyle"
            TargetType="Border">
        <Setter Property="Background" Value="{StaticResource InstallCardBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="#1A2E4A"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="18"/>
        <Setter Property="Padding" Value="26"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="ClipToBounds" Value="False"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1"
                                ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="16"
                                  ShadowDepth="0"
                                  Color="#050A18"
                                  Opacity="0.2"
                                  RenderingBias="Performance"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallInteractiveCardBorderStyle"
           TargetType="Border"
           BasedOn="{StaticResource InstallCardBorderStyle}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource InstallCardHoverBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource InstallCardHoverBorderBrush}"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="18"
                                          ShadowDepth="0"
                                          Color="#0A1224"
                                          Opacity="0.32"
                                          RenderingBias="Performance"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <x:Array x:Key="InstallHubBundleSkeletonItems" Type="{x:Type sys:Int32}">
        <sys:Int32>0</sys:Int32>
        <sys:Int32>1</sys:Int32>
        <sys:Int32>2</sys:Int32>
        <sys:Int32>3</sys:Int32>
    </x:Array>

    <x:Array x:Key="InstallHubPackageSkeletonItems" Type="{x:Type sys:Int32}">
        <sys:Int32>0</sys:Int32>
        <sys:Int32>1</sys:Int32>
        <sys:Int32>2</sys:Int32>
        <sys:Int32>3</sys:Int32>
        <sys:Int32>4</sys:Int32>
        <sys:Int32>5</sys:Int32>
    </x:Array>

    <x:Array x:Key="InstallHubQueueSkeletonItems" Type="{x:Type sys:Int32}">
        <sys:Int32>0</sys:Int32>
        <sys:Int32>1</sys:Int32>
        <sys:Int32>2</sys:Int32>
    </x:Array>

    <DataTemplate x:Key="InstallHubBundleSkeletonTemplate">
        <Border Background="#0F172A"
                BorderBrush="#1E293B"
                BorderThickness="1"
                CornerRadius="14"
                Padding="18"
                Margin="0,0,22,22">
            <StackPanel>
                <Rectangle Height="16" Width="130" RadiusX="4" RadiusY="4" Fill="#193150"/>
                <Rectangle Height="12" Width="180" Margin="0,10,0,0" RadiusX="4" RadiusY="4" Fill="#12263D"/>
                <Rectangle Height="12" Width="120" Margin="0,10,0,0" RadiusX="4" RadiusY="4" Fill="#12263D"/>
            </StackPanel>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="InstallHubPackageSkeletonTemplate">
        <Border Background="#0F172A"
                BorderBrush="#1E293B"
                BorderThickness="1"
                CornerRadius="12"
                Padding="18"
                Margin="0,0,0,14">
            <StackPanel>
                <Rectangle Height="16" Width="200" RadiusX="4" RadiusY="4" Fill="#193150"/>
                <Rectangle Height="12" Width="160" Margin="0,8,0,0" RadiusX="4" RadiusY="4" Fill="#12263D"/>
                <Rectangle Height="12" Width="240" Margin="0,12,0,0" RadiusX="4" RadiusY="4" Fill="#12263D"/>
            </StackPanel>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="InstallHubQueueSkeletonTemplate">
        <Border Background="#0F172A"
                BorderBrush="#1E293B"
                BorderThickness="1"
                CornerRadius="10"
                Padding="16"
                Margin="0,0,0,12">
            <StackPanel>
                <Rectangle Height="14" Width="200" RadiusX="4" RadiusY="4" Fill="#193150"/>
                <Rectangle Height="12" Width="150" Margin="0,8,0,0" RadiusX="4" RadiusY="4" Fill="#12263D"/>
            </StackPanel>
        </Border>
    </DataTemplate>

    <Style x:Key="InstallBundleCardItemStyle"
           TargetType="ListBoxItem">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,0,16,16"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Focusable" Value="False"/>
    </Style>

    <Style x:Key="InstallPackageCardItemStyle"
           TargetType="ListViewItem">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,0,22,22"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border x:Name="Container"
                            CornerRadius="16"
                            Background="#0F1A30"
                            BorderBrush="#213552"
                            BorderThickness="1"
                            Padding="20"
                            SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Container" Property="BorderBrush" Value="#38BDF8"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Container" Property="Background" Value="#162643"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Container" Property="Opacity" Value="0.55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InstallQueueListItemStyle"
           TargetType="ListViewItem">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border x:Name="Container"
                            CornerRadius="12"
                            Background="#1E293B"
                            BorderBrush="#334155"
                            BorderThickness="1"
                            Padding="16"
                            SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Container" Property="Background" Value="#263548"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Container" Property="BorderBrush" Value="#38BDF8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <DataTemplate x:Key="InstallOperationDetailsTemplate"
                  DataType="{x:Type viewModels:InstallOperationItemViewModel}">
        <StackPanel>
            <TextBlock Text="{Binding PackageName}"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource InstallTextPrimaryBrush}"
                       TextWrapping="Wrap"/>

            <StackPanel Orientation="Horizontal"
                        Margin="0,8,0,0"
                        VerticalAlignment="Center">
                <Border Background="{StaticResource InstallBadgeAltBackgroundBrush}"
                        CornerRadius="6"
                        Padding="8,3">
                    <TextBlock Text="{Binding StatusLabel}"
                               FontSize="12"
                               FontWeight="SemiBold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource InstallTextAccentBrush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding StatusLabel}" Value="Installed">
                                        <Setter Property="Foreground" Value="{StaticResource InstallTextSuccessBrush}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding StatusLabel}" Value="Failed">
                                        <Setter Property="Foreground" Value="{StaticResource InstallTextDangerBrush}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding StatusLabel}" Value="Cancelled">
                                        <Setter Property="Foreground" Value="{StaticResource InstallTextWarningBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Border>

                <TextBlock Text="{Binding CompletedAt, StringFormat=Completed · {0:G}}"
                           Margin="12,0,0,0"
                           Foreground="{StaticResource InstallTextCaptionBrush}"
                           FontSize="12"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Text="{Binding Attempts}"
                           Margin="12,0,0,0"
                           Foreground="{StaticResource InstallTextSecondaryBrush}"
                           FontSize="11"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>

            <TextBlock Text="{Binding Message}"
                       Foreground="{StaticResource InstallTextSecondaryBrush}"
                       FontSize="13"
                       TextWrapping="Wrap"
                       Margin="0,10,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <Border Margin="0,14,0,0"
                    Background="#0F172A"
                    BorderBrush="#1E293B"
                    BorderThickness="1"
                    CornerRadius="10">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Padding" Value="14"/>
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasTranscript}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <StackPanel Orientation="Horizontal"
                                Visibility="{Binding HasOutput, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Output"
                                   FontSize="12"
                                   Foreground="{StaticResource InstallTextInfoBrush}"
                                   FontWeight="SemiBold"/>
                    </StackPanel>

                    <ItemsControl ItemsSource="{Binding OutputLines}"
                                  Margin="0,6,0,0"
                                  Visibility="{Binding HasOutput, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           Foreground="{StaticResource InstallTextInverseBrush}"
                                           FontSize="12"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,6"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Separator Margin="0,8,0,8"
                               Visibility="{Binding HasErrors, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <StackPanel Orientation="Horizontal"
                                Visibility="{Binding HasErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Errors"
                                   FontSize="12"
                                   Foreground="{StaticResource InstallTextDangerAccentBrush}"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                    <ItemsControl ItemsSource="{Binding ErrorLines}"
                                  Margin="0,6,0,0"
                                  Visibility="{Binding HasErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           Foreground="{StaticResource InstallTextDangerSoftBrush}"
                                           FontSize="12"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,6"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </DataTemplate>

    <Style x:Key="InstallHubPivotButtonStyle"
           TargetType="ToggleButton">
        <Setter Property="Padding" Value="18,10"/>
        <Setter Property="MinWidth" Value="150"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource InstallTextInverseBrush}"/>
        <Setter Property="Background" Value="{StaticResource InstallSecondaryButtonBrush}"/>
        <Setter Property="BorderBrush" Value="#2A3C5A"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Root"
                            CornerRadius="20"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallSecondaryButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource InstallAccentButtonBrush}"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource InstallTextAccentBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource InstallTextPrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.65"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
