<Page x:Class="OptiSys.App.Views.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:svc="clr-namespace:OptiSys.App.Services"
      xmlns:viewModels="clr-namespace:OptiSys.App.ViewModels"
      xmlns:controls="clr-namespace:OptiSys.App.Views"
      mc:Ignorable="d"
      Title="Logs"
      Background="Transparent"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True"
      TextOptions.TextFormattingMode="Display"
      TextOptions.TextRenderingMode="ClearType"
      TextOptions.TextHintingMode="Fixed"
      RenderOptions.BitmapScalingMode="HighQuality">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Card Style -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="24"/>
        </Style>

        <!-- Log Entry Card -->
        <Style x:Key="LogEntry" TargetType="Border">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsMouseOver}" Value="True">
                    <Setter Property="Background" Value="#263548"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Level Badge -->
        <Style x:Key="LevelBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Background" Value="#1E3A5F"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static svc:ActivityLogLevel.Success}">
                    <Setter Property="Background" Value="#14532D"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static svc:ActivityLogLevel.Warning}">
                    <Setter Property="Background" Value="#713F12"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static svc:ActivityLogLevel.Error}">
                    <Setter Property="Background" Value="#7F1D1D"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Level Text Color -->
        <Style x:Key="LevelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#38BDF8"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static svc:ActivityLogLevel.Success}">
                    <Setter Property="Foreground" Value="#4ADE80"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static svc:ActivityLogLevel.Warning}">
                    <Setter Property="Foreground" Value="#FBBF24"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static svc:ActivityLogLevel.Error}">
                    <Setter Property="Foreground" Value="#F87171"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#475569"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#8B5CF6"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#A78BFA"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close Button -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="Transparent" CornerRadius="8">
                            <TextBlock Text="✕" FontSize="16" Foreground="#94A3B8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#334155"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Info Chip -->
        <Style x:Key="InfoChip" TargetType="Border">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,0,12,8"/>
        </Style>
    </Page.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <controls:PageTitleBar Title="Activity Logs" Subtitle="Review automation output and key events" IconGlyph="📋" Margin="0,0,0,24"/>

        <!-- Main Content - Full Width -->
        <Border Grid.Row="1" Style="{StaticResource Card}">
            <ScrollViewer x:Name="LeftScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Filters -->
                    <WrapPanel Margin="0,0,0,16">
                        <TextBox Style="{StaticResource App.TextBoxStyle}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=150}" Width="220" VerticalAlignment="Center" Margin="0,0,12,8">
                            <TextBox.Tag>Search logs...</TextBox.Tag>
                        </TextBox>

                        <ComboBox Style="{StaticResource App.ComboBoxStyle}" ItemsSource="{Binding LevelFilterOptions}" SelectedItem="{Binding SelectedLevel}" Width="140" Margin="0,0,12,8">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="All levels"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding}" Value="{x:Static svc:ActivityLogLevel.Information}">
                                                        <Setter Property="Text" Value="ℹ️ Info"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding}" Value="{x:Static svc:ActivityLogLevel.Success}">
                                                        <Setter Property="Text" Value="✅ Success"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding}" Value="{x:Static svc:ActivityLogLevel.Warning}">
                                                        <Setter Property="Text" Value="⚠️ Warning"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding}" Value="{x:Static svc:ActivityLogLevel.Error}">
                                                        <Setter Property="Text" Value="❌ Error"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <CheckBox Style="{StaticResource App.CheckBoxStyle}" Content="Auto-select latest" IsChecked="{Binding IsAutoSelectLatest}" VerticalAlignment="Center" Foreground="#94A3B8" Margin="0,0,0,8"/>
                    </WrapPanel>

                    <!-- Stats Row -->
                    <TextBlock Grid.Row="1" Text="{Binding Items.Count, ElementName=LogsList, StringFormat={}{0} entries}" Foreground="#64748B" FontSize="12" Margin="0,0,0,16"/>

                    <!-- Log List -->
                    <ListView x:Name="LogsList" Grid.Row="2" ItemsSource="{Binding EntriesView}" SelectedItem="{Binding SelectedEntry}" Background="Transparent" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingStackPanel.ScrollUnit="Pixel" PreviewMouseWheel="OnListViewPreviewMouseWheel">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,10"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>

                        <ListView.ItemTemplate>
                            <DataTemplate DataType="{x:Type viewModels:ActivityLogItemViewModel}">
                                <Border Style="{StaticResource LogEntry}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                                <Border Style="{StaticResource LevelBadge}">
                                                    <TextBlock Text="{Binding LevelDisplay}" Style="{StaticResource LevelText}"/>
                                                </Border>
                                                <TextBlock Text="{Binding TimestampDisplay}" Foreground="#64748B" FontSize="11" Margin="12,0,0,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Source}" Foreground="#475569" FontSize="11" Margin="12,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="200"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Message}" Foreground="#E2E8F0" FontSize="13" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                        <!-- View Details Button -->
                                        <Button Grid.Column="1" Content="View details" Click="OnViewDetailsClick" Tag="{Binding}" Style="{StaticResource ActionButton}" VerticalAlignment="Center" Margin="16,0,8,0"/>

                                        <!-- Copy Button -->
                                        <Button Grid.Column="2" Content="📋" Command="{Binding DataContext.CopyEntryCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" Style="{StaticResource ActionButton}" ToolTip="Copy to clipboard" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Empty State -->
                    <TextBlock Grid.Row="2" Text="No log entries. Run an operation to see results here." Foreground="#475569" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Items.Count, ElementName=LogsList}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </ScrollViewer>
        </Border>

        <!-- Details Popup Overlay -->
        <Grid x:Name="DetailsOverlay" Grid.RowSpan="2" Visibility="Collapsed">
            <!-- Backdrop -->
            <Border Background="#000000" Opacity="0.6" MouseDown="OnOverlayBackdropClick"/>

            <!-- Dialog Container - Centered and responsive -->
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="700" MinWidth="400" MaxHeight="600" Margin="40">
                <Border Background="#0F172A" CornerRadius="20" Padding="28">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.5" Color="#000000"/>
                    </Border.Effect>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="Log Entry Details" FontSize="18" FontWeight="SemiBold" Foreground="#F1F5F9"/>
                                <TextBlock Text="Full information about this activity" FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
                            </StackPanel>

                            <Button Grid.Column="1" Style="{StaticResource CloseButton}" Click="OnCloseDetailsClick" ToolTip="Close"/>
                        </Grid>

                        <!-- Content with ScrollViewer -->
                        <ScrollViewer x:Name="RightScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel>
                                <!-- Meta Chips -->
                                <WrapPanel Margin="0,0,0,20">
                                    <Border Style="{StaticResource InfoChip}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Level:" Foreground="#64748B" FontSize="12"/>
                                            <TextBlock x:Name="DetailLevel" Foreground="#E2E8F0" FontSize="12" FontWeight="SemiBold" Margin="6,0,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource InfoChip}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Time:" Foreground="#64748B" FontSize="12"/>
                                            <TextBlock x:Name="DetailTime" Foreground="#E2E8F0" FontSize="12" Margin="6,0,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource InfoChip}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Source:" Foreground="#64748B" FontSize="12"/>
                                            <TextBlock x:Name="DetailSource" Foreground="#E2E8F0" FontSize="12" Margin="6,0,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </WrapPanel>

                                <!-- Message Section -->
                                <TextBlock Text="Message" Foreground="#64748B" FontSize="12" FontWeight="Medium" Margin="0,0,0,8"/>
                                <Border Background="#1E293B" CornerRadius="12" Padding="16" Margin="0,0,0,20">
                                    <TextBlock x:Name="DetailMessage" Foreground="#E2E8F0" FontSize="14" TextWrapping="Wrap" LineHeight="22"/>
                                </Border>

                                <!-- Details Section -->
                                <TextBlock Text="Additional Details" Foreground="#64748B" FontSize="12" FontWeight="Medium" Margin="0,0,0,8"/>
                                <Border Background="#1E293B" CornerRadius="12" Padding="16" MinHeight="80">
                                    <StackPanel x:Name="DetailsList"/>
                                </Border>
                                <TextBlock x:Name="NoDetailsText" Text="No additional details available" Foreground="#475569" FontSize="12" Margin="0,8,0,0" Visibility="Collapsed"/>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Footer Actions -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                            <Button x:Name="CopyDetailsButton" Content="📋 Copy to clipboard" Style="{StaticResource PrimaryButton}" Click="OnCopyDetailsClick"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Border>
        </Grid>
    </Grid>
</Page>
