<Page x:Class="OptiSys.App.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="clr-namespace:OptiSys.App.Views"
      mc:Ignorable="d"
      Background="Transparent"
      Title="Settings"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True"
      TextOptions.TextFormattingMode="Display"
      TextOptions.TextRenderingMode="ClearType"
      TextOptions.TextHintingMode="Fixed"
      RenderOptions.BitmapScalingMode="HighQuality">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Modern Toggle Switch -->
        <Style x:Key="ModernToggleStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Track" Background="#1E293B" CornerRadius="12" BorderThickness="0"/>
                            <Ellipse x:Name="Thumb" Width="18" Height="18" Fill="#64748B" HorizontalAlignment="Left" Margin="3,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#8B5CF6"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="#FFFFFF"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#334155"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Track" Property="Background" Value="#A78BFA"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Track" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section Card -->
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Section Title -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#F1F5F9"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Section Subtitle -->
        <Style x:Key="SectionSubtitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <!-- Setting Row -->
        <Style x:Key="SettingRow" TargetType="Grid">
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Primary Button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#8B5CF6"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#A78BFA"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#334155"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Info Chip -->
        <Style x:Key="InfoChip" TargetType="Border">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
        </Style>

        <!-- Status Badge -->
        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="Background" Value="#22C55E"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10,4"/>
        </Style>

        <!-- Danger Button -->
        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#7F1D1D"/>
            <Setter Property="Foreground" Value="#FCA5A5"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#991B1B"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small Secondary Button -->
        <Style x:Key="SmallSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#CBD5E1"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#334155"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="32">
            <StackPanel>

                <!-- Page Header -->
                <controls:PageTitleBar Title="Settings" Subtitle="Configure how OptiSys runs on your machine" IconGlyph="⚙️" Margin="0,0,0,32"/>

                <!-- Updates Section -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Text="Updates" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="Check for new releases and install updates" Style="{StaticResource SectionSubtitle}"/>

                        <!-- Version Info -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Style="{StaticResource InfoChip}">
                                <StackPanel>
                                    <TextBlock Text="Current" FontSize="11" Foreground="#64748B"/>
                                    <TextBlock Text="{Binding CurrentVersionDisplay}" FontSize="15" FontWeight="SemiBold" Foreground="#F1F5F9"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource InfoChip}">
                                <StackPanel>
                                    <TextBlock Text="Latest" FontSize="11" Foreground="#64748B"/>
                                    <TextBlock Text="{Binding LatestVersionDisplay}" FontSize="15" FontWeight="SemiBold" Foreground="#F1F5F9"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Style="{StaticResource InfoChip}" HorizontalAlignment="Left">
                                <StackPanel>
                                    <TextBlock Text="Last checked" FontSize="11" Foreground="#64748B"/>
                                    <TextBlock Text="{Binding LastUpdateCheckDisplay}" FontSize="13" Foreground="#94A3B8"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- Action Buttons -->
                        <StackPanel Orientation="Horizontal">
                            <Button Content="{Binding CheckForUpdatesButtonLabel}" Command="{Binding CheckForUpdatesCommand}" Style="{StaticResource PrimaryButton}" IsEnabled="{Binding IsUpdateActionsEnabled}"/>
                            <Button Content="{Binding InstallUpdateButtonLabel}" Command="{Binding InstallUpdateCommand}" Style="{StaticResource SecondaryButton}" Margin="12,0,0,0" IsEnabled="{Binding IsUpdateActionsEnabled}" Visibility="{Binding IsInstallUpdateVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>

                        <!-- Progress -->
                        <ProgressBar Height="3" Margin="0,16,0,0" IsIndeterminate="True" Background="#1E293B" Foreground="#8B5CF6" Visibility="{Binding IsCheckingForUpdates, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <StackPanel Margin="0,12,0,0" Visibility="{Binding ShowInstallerProgress, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ProgressBar Height="3" Maximum="100" Value="{Binding InstallerDownloadProgress, Mode=OneWay}" Background="#1E293B" Foreground="#8B5CF6" IsIndeterminate="{Binding IsInstallerProgressIndeterminate, Mode=OneWay}"/>
                            <TextBlock Text="{Binding InstallerDownloadStatus}" Foreground="#64748B" FontSize="12" Margin="0,6,0,0"/>
                        </StackPanel>

                        <TextBlock Text="{Binding UpdateStatusMessage}" Foreground="#64748B" FontSize="12" Margin="0,12,0,0" TextWrapping="Wrap"/>

                        <!-- Release Notes -->
                        <StackPanel Orientation="Horizontal" Margin="0,16,0,0" Visibility="{Binding HasReleaseNotes, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Button Content="📄 View release notes" Command="{Binding ShowReleaseNotesCommand}" Style="{StaticResource SecondaryButton}"/>
                            <Button Content="Open on GitHub" Command="{Binding OpenReleaseNotesLinkCommand}" Style="{StaticResource SecondaryButton}" Margin="12,0,0,0" IsEnabled="{Binding HasReleaseNotesLink}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Elevation Section -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Text="Elevation" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="OptiSys requires admin privileges for repairs and system changes" Style="{StaticResource SectionSubtitle}"/>

                        <Border Background="#0F172A" CornerRadius="12" Padding="16" BorderBrush="#1E293B" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock Text="{Binding CurrentPrivilegeDisplay}" FontSize="15" FontWeight="SemiBold" Foreground="#F1F5F9"/>
                                    <TextBlock Text="{Binding CurrentPrivilegeAdvice}" Foreground="#64748B" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <Border Grid.Column="1" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border" BasedOn="{StaticResource StatusBadge}">
                                            <Setter Property="Background" Value="#22C55E"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRunningElevated}" Value="False">
                                                    <Setter Property="Background" Value="#EF4444"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="SemiBold" Foreground="#0F172A">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="ELEVATED"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsRunningElevated}" Value="False">
                                                        <Setter Property="Text" Value="LIMITED"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Behavior Section -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Text="Behavior" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="Control how the app starts and runs in the background" Style="{StaticResource SectionSubtitle}"/>

                        <!-- Run in background -->
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Run in background" Foreground="#E2E8F0" FontSize="14" FontWeight="Medium"/>
                                <TextBlock Text="Minimize to tray instead of closing" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding RunInBackground, Mode=TwoWay}"/>
                        </Grid>

                        <!-- Launch at startup -->
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Launch at startup" Foreground="#E2E8F0" FontSize="14" FontWeight="Medium"/>
                                <TextBlock Text="Start OptiSys when you log in" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding LaunchAtStartup, Mode=TwoWay}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- PulseGuard Section -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="PulseGuard" Style="{StaticResource SectionTitle}" Margin="0"/>
                            <Border Background="#8B5CF6" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="WATCHDOG" FontSize="9" FontWeight="Bold" Foreground="#FFFFFF"/>
                            </Border>
                        </StackPanel>
                        <TextBlock Text="Get notified about automation results and system issues" Style="{StaticResource SectionSubtitle}"/>

                        <!-- Enable PulseGuard -->
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Enable PulseGuard" Foreground="#E2E8F0" FontSize="14" FontWeight="Medium"/>
                                <TextBlock Text="Monitor automation and surface important events" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding PulseGuardEnabled, Mode=TwoWay}"/>
                        </Grid>

                        <!-- Notifications -->
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Show notifications" Foreground="#E2E8F0" FontSize="14" FontWeight="Medium"/>
                                <TextBlock Text="Display Windows toasts for events" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding NotificationsEnabled, Mode=TwoWay}" IsEnabled="{Binding PulseGuardEnabled}"/>
                        </Grid>

                        <!-- Only when away -->
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Only notify when away" Foreground="#E2E8F0" FontSize="14" FontWeight="Medium"/>
                                <TextBlock Text="Mute toasts while the window is focused" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding NotifyOnlyWhenInactive, Mode=TwoWay}" IsEnabled="{Binding CanAdjustPulseGuardNotifications}"/>
                        </Grid>

                        <!-- Success summaries -->
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Show completion digests" Foreground="#E2E8F0" FontSize="14" FontWeight="Medium"/>
                                <TextBlock Text="Get a summary when scripts finish" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding PulseGuardShowSuccessSummaries, Mode=TwoWay}" IsEnabled="{Binding CanAdjustPulseGuardNotifications}"/>
                        </Grid>

                        <!-- Action alerts -->
                        <Grid Margin="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Show action-required alerts" Foreground="#E2E8F0" FontSize="14" FontWeight="Medium"/>
                                <TextBlock Text="Alert for blockers like pending restarts" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <ToggleButton Grid.Column="1" Style="{StaticResource ModernToggleStyle}" IsChecked="{Binding PulseGuardShowActionAlerts, Mode=TwoWay}" IsEnabled="{Binding CanAdjustPulseGuardNotifications}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- App Data Locations Section -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Text="App Data Locations" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="View and manage files stored by OptiSys on your system" Style="{StaticResource SectionSubtitle}"/>

                        <ItemsControl ItemsSource="{Binding DataLocations}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#0B1120" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" Foreground="#F1F5F9"/>
                                                    <Border Background="#1E293B" CornerRadius="6" Padding="8,3" Margin="10,0,0,0" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding SizeDisplay}" FontSize="11" Foreground="#94A3B8"/>
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                                                <TextBlock Text="{Binding Path}" FontSize="11" Foreground="#475569" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Content="Open" Command="{Binding OpenCommand}" Style="{StaticResource SmallSecondaryButton}" Margin="0,0,8,0" IsEnabled="{Binding Exists}"/>
                                                <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}" IsEnabled="{Binding Exists}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Release Notes Overlay -->
        <Grid x:Name="ReleaseNotesOverlay" Panel.ZIndex="10" Background="#CC0F172A" Visibility="{Binding IsReleaseNotesDialogVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border MaxWidth="700" MaxHeight="600" Margin="48" Background="#0F172A" CornerRadius="20" Padding="28" BorderBrush="#1E293B" BorderThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="{Binding ReleaseNotesDialogTitle}" FontSize="18" FontWeight="SemiBold" Foreground="#F1F5F9"/>
                        <TextBlock Text="{Binding ReleaseNotesDialogSubtitle}" Foreground="#64748B" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>

                    <Border Grid.Row="1" Background="#0B1120" CornerRadius="12" Padding="16">
                        <FlowDocumentScrollViewer Document="{Binding ReleaseNotesDocument}" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" IsSelectionEnabled="True" FontSize="13" Foreground="#E2E8F0" Background="Transparent" MaxHeight="400"/>
                    </Border>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Open on GitHub" Command="{Binding OpenReleaseNotesLinkCommand}" Style="{StaticResource SecondaryButton}" IsEnabled="{Binding HasReleaseNotesLink}"/>
                        <Button Content="Copy" Command="{Binding CopyReleaseNotesCommand}" Style="{StaticResource SecondaryButton}" Margin="12,0"/>
                        <Button Content="Close" Command="{Binding CloseReleaseNotesCommand}" Style="{StaticResource PrimaryButton}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
